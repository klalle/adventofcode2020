import kotlin.system.measureTimeMillis

fun main(args: Array<String>) {
//    println("${measureTimeMillis {Nine().run()}}ms")
    println("${measureTimeMillis {Eleven().run()}}ms")
    /*val oneA=measureTimeMillis { One().run() }
    val oneB=measureTimeMillis { One().run2() }
    val twoA=measureTimeMillis { Two().run() }
    val twoB=measureTimeMillis { Two().run2() }
    val threeA=measureTimeMillis { Three().run() }
    val threeB=measureTimeMillis { Three().run2() }
    val fourA=measureTimeMillis { Four().run() }
    val fourB=measureTimeMillis { Four().run2() }
    val fiveA=measureTimeMillis { Five().run() }
    val fiveB=measureTimeMillis { Five().run2() }
    val sixA=measureTimeMillis { Six().run() }
    val sixB=measureTimeMillis { Six().run2() }
    val sevenA=measureTimeMillis { Seven().run() }
    val sevenB=measureTimeMillis { Seven().run2() }
    val eightA=measureTimeMillis { Eight().run() }
    val eightB=measureTimeMillis { Eight().run2() }
    val nineA=measureTimeMillis { Nine().run() }
    val nineB=measureTimeMillis { Nine().run2() }

    println("ExecutionTime: 1.a $oneA ms    1.b $oneB ms")
    println("ExecutionTime: 2.a $twoA ms    2.b $twoB ms")
    println("ExecutionTime: 3.a $threeA ms     3.b $threeB ms")
    println("ExecutionTime: 4.a $fourA ms     4.b $fourB ms")
    println("ExecutionTime: 5.a $fiveA ms     5.b $fiveB ms")
    println("ExecutionTime: 6.a $sixA ms     6.b $sixB ms")
    println("ExecutionTime: 7.a $sevenA ms  7.b $sevenB ms")
    println("ExecutionTime: 8.a $eightA ms     8.b $eightB ms")
    println("ExecutionTime: 9.a $nineA ms    9.b $nineB ms")*/
}
